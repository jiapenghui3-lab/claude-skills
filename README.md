# Claude Skills

个人 Claude Code slash command 集合。

## 已有 skill

| 命令 | 说明 |
|------|------|
| `/tutor` | Bloom 2-Sigma 一对一学习系统，支持任意主题，自动管理学习进度 |

## 使用方式

把 `commands/` 目录下的 `.md` 文件复制到 `~/.claude/commands/`，重启 Claude Code 即可使用对应的 slash command。
